{
  "electron": "concurrently \"npm run dev\" \"wait-on http://localhost:8080 && electron electron/main.js\"",
  "electron:build": "npm run build && tsc electron/*.ts --outDir electron && electron-builder",
  "electron:dev": "tsc electron/*.ts --outDir electron && npm run electron"
}